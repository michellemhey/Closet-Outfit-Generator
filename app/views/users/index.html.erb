<div class="container">
  <div class="row offset2">

			<ul class="users">
			  <% @users.each do |user| %>
			    <li>
			      <%= link_to user.name, user %> signed up %> 
			      <%= user.created_at.to_date %>
			    </li>
			  <% end %>
			</ul>

	</div>
</div>